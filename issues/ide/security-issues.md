# Tauri Desktop App — Security Issues

> Package: `packages/desktop/`
> Generated: 2026-03-28
> Verified: 2026-03-28
> Scope: Tauri config, capabilities, entitlements, Rust backend, markdown parser, build scripts

---

## Critical

| #   | Issue                                                 | File                                  | Line(s) | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ----------------------------------------------------- | ------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **CSP disabled**                                      | `src-tauri/tauri.conf.json`           | 22      | `"csp": null` — No Content Security Policy set. Webview can load scripts, styles, frames, and resources from any origin. Nullifies browser-level XSS mitigations. Neither `tauri.beta.conf.json` nor `tauri.prod.conf.json` override this. **Fix:** Set restrictive CSP: `"csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' https://api.kilo.ai https://*.kilo.ai"`                                                                                               |
| 2   | **Unrestricted filesystem open access**               | `src-tauri/capabilities/default.json` | 10-19   | `opener:allow-open-path` grants `**/*`, `/**/*`, `**/.*/*/**`, and `/**/.*/*/**` — webview can open any file on disk including `.ssh`, `.gnupg`, `.env`. A compromised renderer can exfiltrate sensitive files. **Fix:** Restrict to specific directories the app legitimately needs.                                                                                                                                                                                                                               |
| 3   | **HTTP allowed to any host**                          | `src-tauri/capabilities/default.json` | 47-49   | `http:default` permission overridden with `{ "url": "http://*" }` — webview can make requests to any host over unencrypted HTTP. Enables MITM attacks, credential interception, data tampering. **Fix:** Remove `http://*` entries; allow only specific HTTPS endpoints.                                                                                                                                                                                                                                            |
| 4   | **macOS code signing protections all disabled**       | `src-tauri/entitlements.plist`        | 5-14    | Five hardening flags disabled: `cs.allow-jit`, `cs.allow-unsigned-executable-memory`, `cs.disable-executable-page-protection`, `cs.allow-dyld-environment-variables`, `cs.disable-library-validation`. Combined: attacker can inject libraries via `DYLD_INSERT_LIBRARIES`, load unsigned code, execute from writable memory. `allow-jit` and `disable-library-validation` may be needed for sidecar + JS engine; review `allow-dyld-environment-variables` and `disable-executable-page-protection` for necessity. |
| 5   | **XSS via markdown `unsafe=true`**                    | `src-tauri/src/markdown.rs`           | 20, 50  | `options.render.r#unsafe = true` causes the `dangerous_url(url)` guard to be bypassed. `javascript:alert(1)` in a markdown link renders directly in the webview. Markdown input comes from AI assistant processing external/repo content. `rel="noopener noreferrer"` does not prevent `javascript:` execution. **Fix:** Set `unsafe = false`, or always check `dangerous_url` regardless of unsafe flag.                                                                                                           |
| 6   | **Command injection in `wsl_path`**                   | `src-tauri/src/lib.rs`                | 281-315 | Path argument interpolated into shell string via `format!("wslpath {flag} \"$HOME{escaped}\"")`. The `escaped` portion is concatenated into a double-quoted string; closing quotes, `$()`, backticks, or dollar signs are evaluated by the shell. **Fix:** Use the non-tilde branch pattern for all cases — pass path as discrete argument to `wslpath` instead of embedding in `sh -lc` string.                                                                                                                    |
| 7   | **TOCTOU race with fixed temp file**                  | `src-tauri/src/cli.rs`                | 179-194 | Install script written to predictable path `std::env::temp_dir().join("opencode-install.sh")`. Another process could replace the file between `write` and `execute`. On Linux, file permissions are `0755` — any user could execute it. **Fix:** Use unique temp filename (e.g., `format!("opencode-install-{}.sh", uuid())`) or `tempfile::NamedTempFile`.                                                                                                                                                         |
| 8   | **Command injection risk in WSL script construction** | `src-tauri/src/cli.rs`                | 439-468 | WSL branch constructs shell script from inputs. `args` parameter is interpolated directly into the script string. `shell_escape()` helper exists but is only applied to env values and version — not to `args`. Currently `hostname` (String) and `port` (u32) are safe, but `args` is a raw `&str` — any future caller interpolating user input creates an injection vector. **Fix:** Pass args as structured list; shell-escape each argument individually. Add comment warning about injection risk.             |

## High

| #   | Issue                                                   | File                                  | Line(s) | Details                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------- | ------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 9   | **Capabilities apply to all windows**                   | `src-tauri/capabilities/default.json` | 5       | `"windows": ["*"]` grants all capabilities to every window including transient/error windows. Combined with issues #1-3, any webview has full opener, HTTP, and shell access. **Fix:** Restrict to `"windows": ["main"]`.                                                                                       |
| 10  | **`shell:default` permission granted**                  | `src-tauri/capabilities/default.json` | 38      | Includes `shell:allow-open` and `shell:allow-execute`. Combined with #9, any webview can spawn processes. If shell commands are only invoked from Rust, this is unnecessary attack surface. **Fix:** Remove `shell:default`; add specific permissions only if needed.                                           |
| 11  | **TOCTOU race in Windows registry**                     | `src-tauri/src/os/windows.rs`         | 185-204 | Registry value size queried first, then data read. The `kind` check at line 181 runs between the two calls but not after the second `RegGetValueW`. If registry data changes between calls, buffer size may no longer match. Low-probability race. **Fix:** Document assumption or add final consistency check. |
| 12  | **`resolve_cmd` reads entire files with no size limit** | `src-tauri/src/os/windows.rs`         | 272-312 | `.cmd` and `.bat` files read entirely into memory with `std::fs::read(path)`. No size validation before reading. **Fix:** Add size limit check or use buffered reading.                                                                                                                                         |
| 13  | **Unaudited git commits as dependencies**               | `src-tauri/Cargo.toml`                | 70-75   | `specta`, `specta-typescript`, `tauri-specta`, and `tauri` itself are patched to specific git revisions (not tagged releases). No guarantee these commits have been audited; branches could be force-pushed. **Fix:** Pin to tagged releases or fork with audited commits.                                      |

## Medium

| #   | Issue                                                    | File                                                           | Line(s) | Details                                                                                                                                                                                                                                                                    |
| --- | -------------------------------------------------------- | -------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 14  | **UUID v4 not guaranteed crypto-safe for auth password** | `src-tauri/src/lib.rs`                                         | 652     | `uuid::Uuid::new_v4()` does not guarantee cryptographic randomness. Used as HTTP Basic Auth credentials for local sidecar — could be guessed if RNG is weak. **Fix:** Use `getrandom::getrandom()` for generating the password.                                            |
| 15  | **Port 0 race condition**                                | `src-tauri/src/lib.rs`                                         | 667-678 | `TcpListener::bind("127.0.0.1:0")` gets OS-assigned port, then listener is immediately dropped. Another process could bind that port between `drop` and sidecar's bind. **Fix:** Keep `TcpListener` alive until sidecar has bound, or have sidecar report its actual port. |
| 16  | **`withGlobalTauri: true`**                              | `src-tauri/tauri.conf.json`                                    | 20      | Exposes Tauri IPC bridge as `window.__TAURI__` in every frame. Combined with null CSP, any injected script can invoke all granted capabilities.                                                                                                                            |
| 17  | **`macOSPrivateApi: true`**                              | `src-tauri/tauri.conf.json`                                    | 24      | Enables macOS private APIs (`NSWindow.setTransparent`). Will cause App Store rejection. Acceptable if distributed outside App Store but should be documented.                                                                                                              |
| 18  | **`webkit2gtk` pinned to exact version**                 | `src-tauri/Cargo.toml`                                         | 63      | `webkit2gtk = "=2.0.2"` — exact pinning prevents security updates. Verify this is intentional.                                                                                                                                                                             |
| 19  | **Beta/prod share same updater signing key**             | `src-tauri/tauri.beta.conf.json:29`, `tauri.prod.conf.json:34` | —       | A beta build could be signed as production. Consider separate keys for isolation.                                                                                                                                                                                          |
| 20  | **`unsafe set_var` in build.rs without comment**         | `src-tauri/build.rs`                                           | 3-4     | `unsafe { std::env::set_var("TAURI_CONFIG", ...) }` — pragmatic workaround for externalBin on Windows dev but should have a comment explaining why it's necessary. Note: context exists in Cargo.toml:74-75 (`# TODO: ...`) but not in build.rs itself.                    |

## Summary

| Severity  | Count  |
| --------- | ------ |
| Critical  | 8      |
| High      | 5      |
| Medium    | 7      |
| **Total** | **20** |
